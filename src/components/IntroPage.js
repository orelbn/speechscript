import React from 'react';
import Container from '@mui/material/Container';

export default function IntroPage(props) {
    return (
        <Container maxWidth={false} disableGutters={true} sx={{height: "100vh", scrollSnapAlign: "center", backgroundColor: "#e1e1e1"}}>
        </Container>
    )
}